<!--成绩页-->
<view class="content">
  <view class="{{ scoreClass }}">{{score>60?"恭喜你,":"很遗憾,"}}{{score}}分！</view>
  <view wx:if="{{bestScore}}" class="bestScore">历史最高成绩： {{bestScore}}分</view>
  <view wx:if="{{score>60}}" class="excellent">完美答题（6秒内）：{{ excellent }}个</view>
</view>
